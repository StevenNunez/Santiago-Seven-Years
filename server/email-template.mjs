export function escapeHtml(value) {
  return String(value).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[c]);
}
export function createInvitationEmail(invitation, event, details, origin) {
  const url = new URL('/', origin);
  if (url.protocol !== 'https:') throw new Error('Public invitation URL must use HTTPS');
  url.searchParams.set('invite', invitation.token);
  const time = value => value ? new Intl.DateTimeFormat('es-CL', { hour: '2-digit', minute: '2-digit', hourCycle: 'h23', timeZone: 'America/Santiago' }).format(new Date(value)) : 'por confirmar';
  const date = event.starts_at ? new Intl.DateTimeFormat('es-CL', { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric', timeZone: 'America/Santiago' }).format(new Date(event.starts_at)) : 'sábado 26 de septiembre de 2026';
  const schedule = `${time(event.starts_at)} a ${time(event.ends_at)} hrs`;
  const name = invitation.recipient_name;
  const text = `¡Hola, ${name}! 💙\n\n¡Santiago llega al nivel 7 y quiere celebrarlo contigo!\n\n${date}\n${schedule}\n${details.address || 'Lugar por confirmar'}\n\nAbre tu invitación y confirma tu asistencia:\n${url.href}\n\nGuarda este enlace: también te servirá para compartir fotos de la fiesta. ¡Te esperamos!`;
  const html = `<!doctype html><html lang="es"><head><meta charset="utf-8"></head><body style="margin:0;background:#f2f5fd;font-family:Arial,sans-serif;color:#162744"><table role="presentation" width="100%" cellspacing="0" cellpadding="0"><tr><td align="center" style="padding:28px 12px"><table role="presentation" width="560" cellspacing="0" cellpadding="0" style="max-width:560px;width:100%;background:white;border-radius:18px;overflow:hidden"><tr><td style="background:#0849db;color:white;text-align:center;padding:28px"><p style="font-size:12px;letter-spacing:3px;color:#ffcf38">UNA AVENTURA NIVEL 7</p><h1 style="font-size:34px;margin:16px 0">¡${escapeHtml(name)},<br>esta aventura es contigo!</h1><p>¡Santiago cumple 7 años!</p></td></tr><tr><td><img src="cid:santiago-level7" alt="Sonic te invita a la aventura de Santiago" width="560" style="width:100%;height:auto;display:block"></td></tr><tr><td style="padding:30px;text-align:center"><h2 style="color:#0849db">${escapeHtml(date)}</h2><p style="line-height:1.8">${escapeHtml(schedule)}<br>${escapeHtml(details.address || 'Lugar por confirmar')}</p><p>Nos encantaría celebrar contigo y tu familia.</p><p style="margin:30px 0"><a href="${escapeHtml(url.href)}" style="background:#ffcf38;color:#132854;text-decoration:none;padding:16px 22px;border-radius:9px;display:inline-block;font-weight:bold">Ver mi invitación y confirmar →</a></p><p style="font-size:13px;color:#68758c;line-height:1.7">Guarda este enlace para volver a tu invitación y compartir recuerdos de la fiesta.</p><p style="font-size:12px;word-break:break-all"><a href="${escapeHtml(url.href)}" style="color:#0849db">${escapeHtml(url.href)}</a></p></td></tr></table><p style="font-size:11px;color:#68758c">Hecho con cariño · Santiago nivel 7</p></td></tr></table></body></html>`;
  return { subject: `¡${name}, Santiago te invita a su nivel 7!`, text, html };
}
